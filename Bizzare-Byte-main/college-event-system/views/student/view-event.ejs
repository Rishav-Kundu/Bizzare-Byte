<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= event.name %> — Event Details</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <div class="student-header">
    <h1><%= event.name %></h1>
    <a href="<%= backUrl %>" class="btn btn-secondary">← Back to events</a>
  </div>

  <div class="container">
    <div class="event-detail-card">
      <h2><%= event.name %></h2>
      <% if (event.category) { %>
        <p class="note-text">Category: <strong><%= event.category %></strong></p>
      <% } %>

      <% if (event.date) { %>
        <p><strong>Date:</strong> <%= new Date(event.date).toDateString() %></p>
      <% } %>

      <% if (event.venue) { %>
        <p><strong>Venue:</strong> <%= event.venue %></p>
      <% } %>

      <hr>

      <% if (event.description) { %>
        <div class="event-full-desc"><%= event.description %></div>
      <% } else { %>
        <p class="empty-text">No description provided.</p>
      <% } %>

      <% if (event.registered_count || event.registered) { %>
        <p class="note-text">Registered: <strong><%= event.registered_count || event.registered %></strong></p>
      <% } %>

    </div>
  </div>

</body>
</html>
